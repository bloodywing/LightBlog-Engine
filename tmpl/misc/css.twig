<style type="text/css">

        {# Some common Vars here, you can add more, like a color you use really often
           You don't need to use Twig for CSS, you can do it like the regular way too,
           but it shows you that twig isn't limited to HTML Templates.
            {{% set nice_blue = "#75B4FF" %}}
            .. and use it in any Element you want:

            h3 .somestuff { color: {{ nice_blue }} }
            So you can create some dynamic CSS Templates.
         #}
        {% set contentwidth = "50%" %}
        {% set navigationwidth = "50%" %}
        {% set bgcolor = "#02071F" %}
        {% set linkcolor = "#04114D" %}
        * { -webkit-border-radius: 2px;
            -moz-border-radius: 2px;
            border-radius: 2px;}
        html { color: #000; background: {{ bgcolor }}; padding: 10px }
        body { font: 12px Verdana, Arial, sans-serif; color: #555 }
        img { border: 0; }
        h2 {display: inline-block; margin: 1em 5px 0px 0 }
        #content { width: {{ contentwidth }}; margin:0 auto; padding: 25px; 
                   -webkit-border-radius: 5px;
                   -moz-border-radius: 5px;
                   border-radius: 5px;

        }
        #footer {width: {{ contentwidth }}; margin: 0 auto;}

        /*
        Article Elements
        */
        .article, #forms{background: #fff;
                 -moz-box-shadow: 0px 5px 4px #000;
                 -webkit-box-shadow: 0px 5px 4px #000;
                 box-shadow: 0px 5px 4px #000;
        }
        .article a{color: {{ linkcolor }}}
        .article, #forms{padding: 1em}
        .article, #forms{margin-bottom: 1em}
        .article_tools {margin-top: 1em}
        .article_tools em {display: inline-block}
        .article_tools input {border-style: none}
        
        #forms input {
            border: 1px solid #333; 
            }
        
        /*
        Navigation Elements
        */

        #nav, #pre_footer {
            margin: 0 auto;
            width: {{ navigationwidth }};
            height: 50px;
            background: #fff;
            color: #666;

        }
        
        #pre_footer {
              padding: 2%;
              width: 96%;
              height: 24px;
        }
        
        #nav ul {
            float: left;
            padding-left: 2%;
        }
        #nav a, #pre_footer a {
            color: #fff;
            -webkit-border-radius: 3px;
            -moz-border-radius: 3px;
            border-radius: 3px;
            display: block;
            margin: 0 5px 5px 0;
            padding: 4px 5px;
            float: left;
            font-size: 12px;
            background: {{ bgcolor }};

            text-decoration: none;
        }
        
        #pre_footer a {
            float: none;
            display: inline-block;
        }
        
        #footer * {
            color: #fff;
        }
        
        #nav * {
            list-style: none;
        }
    </style>