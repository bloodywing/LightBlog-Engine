<style type="text/css">

        {# Some common Vars here, you can add more, like a color you use really often
           You don't need to use Twig for CSS, you can do it like the regular way too,
           but it shows you that twig isn't limited to HTML Templates.
            {{% set nice_blue = "#75B4FF" %}}
            .. and use it in any Element you want:

            h3 .somestuff { color: {{ nice_blue }} }
            So you can create some dynamic CSS Templates.
         #}
        {% set contentwidth = "50%" %}
        {% set navigationwidth = "50%" %}
        html { color: #000; background: #eee; padding: 10px }
        body { font: 11px Verdana, Arial, sans-serif; color: #333 }
        img { border: 0; }
        h2 {display: inline-block; margin: 1em 5px 0px 0 }
        #content { width: {{ contentwidth }}; margin:0 auto; padding: 25px; border: 1px solid;
                   background:#fff;
                   -webkit-border-radius: 5px;
                   -moz-border-radius: 5px;
                   border-radius: 5px;

        }
        #footer {width: {{ contentwidth }}; margin: 0 auto;}
        .body {margin: 0 0 0 0}
        .articlehead {background: #fff}
        .article_tools em {display: inline-block}

        /*
        Navigation Elements
        */

        #nav {
            margin: 0 auto 1em;
            width: {{ navigationwidth }};
            background: #000;
            color: #fff;
            height: 5em;

            -webkit-border-radius: 5px;
            -moz-border-radius: 5px;
            border-radius: 5px;
        }
        #nav ul {
            float: left;
        }
        #nav ul * {
            font-size: 1.25em;
        }
        #nav a {
            color: #fff;
            text-decoration: none;
        }
        #nav a:hover {
            color: teal;
            text-decoration: underline; 
        }
        
        
        #nav * {

            list-style: none;
        }
    </style>